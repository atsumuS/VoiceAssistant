
!ВНИМАНИЕ! - Сам ассистент работает при помощи интернет соединения, до офлайна ещё не дошло, но я процессе реализации



1. Установка всех библиотек, что указанны в начале самого кода, 
бе зних голосовой ассистент не будет функционировать 

from neuralintents import GenericAssistant # основная библиотека голосового ассистента
import speech_recognition # бл=иблиотека для голсового общения
import pyttsx3 as tts # библиотека для преобразования текста в речь
import sys # является частью основного стэка 
import nltk # это пакет Python для обработки естественного языка. Для NLTK требуется Python 3.7, 3.8, 3.9 или 3.10.


2. с 8-9 строчки кода идёт обработка языка
    с 11-12 строчки кода идёт установка скорости разговора 
    15 строчка кода - заданые параметры в список для будущей функции todo_list, где ассистент выводит этот список как спиоск ваших дел

3. Дальше идут основные функции голосового асситента, которые отвечают за создание нового файла и добовления дела в уже существующий список дел.

   3.1. с 22-61 строчу кода фнкция, что отвечает за создание файла. (более подробно всё расписание в коментариях в самом коде)

   3.2  Далее с 64-90 идёт функция, котороая отвечает за создание дела и добавления его в уже имеющийся список дел (более подробно всё расписание в коментариях в самом коде)

4. с 101 по 116 строчки кода идут простенькие функции, которые отвечают за диалог с пользователем, которые берут паттерны ответов из JSON(intents.json) файла 

!!!! Сам json файл представлять собой набор патернов(patterns) пользователя, которые распознаёт голосовй ассистент, а голосовй ассистент в свою очередь берёт ответы из (responses)
которыми он отвечает. responses - могут быть прописаны как в коде, так и json файле


5. 119-126 в словаре {mappings} - перечислены функции, если вкратце, то словарь передаётся далее для того, чтобы голосовой ассистент ими отвечал 
    в 130 строчке кода создана пременная, которая сопоставляет со словарём {mappings} значения, туда же предаёт json файл с ответами бота и определёнными словами человека, 
    после которых ассистент как раиз и понимает, что ему надо сделать

6. с 134-148 строку кода идёт бесконечный цикл нашего умопобрачительного общения с ботом



!ВНИМАНИЕ! - ЭТОТ ПРОЕКТ ТЕСТОВЫЙ, В ДАЛЬНЕЙШЕМ Я БУДУ ЕГО ПОПОЛНЯТЬ, ПОКА ОН СДЕЛАН ДЛЯ НАГЛЯДОНСТИ МОИХ ЗНАНИЙ 
